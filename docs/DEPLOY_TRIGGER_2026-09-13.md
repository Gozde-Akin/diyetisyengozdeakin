# Deployment Trigger

This marker commit was created after reconnecting the Vercel project to `Gozde-Akin/diyetisyengozdeakin`.

Purpose: trigger a fresh production deployment from `main` so the private-clinic branding, contact details, SEO and structured-data updates are deployed and verified.
